/*
 * HomePage Messages
 *
 * This contains all the text for the HomePage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.global';

export default defineMessages({
  appName: {
    id: `${scope}.appName`,
    defaultMessage: 'Brahma',
  },
});
